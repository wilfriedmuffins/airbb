<h1> Utilisateurs du site </h1>

<table>
    <thead>
        <tr>
            <th>Id</th>
            <th>Nom</th>
            <th>Email</th>
            <th>Confirmation envoié à</th>
            <th>Confirmé à</th>
        </tr>
    </thead>
    <tbody>
        <% @users.each do |user| %>
            <tr>
                <td><%= user.id %></td>
                <td><%= user.name %></td>
                <td><%= user.email %></td>
                <td><%= user.confirmation_sent_at.try(:strftime, ("%e %B %Y")) %></td>
                <td><%= user.confirmed_at.try(:strftime, ("%e %B %Y")) %></td>
                <%# <td><%= link_to "Show", edit_user_registration_path  </td>%>
        <% end %>
    </tbody>
</table>
